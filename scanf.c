#include<stdio.h>
int main(){
	int x;
	printf("de um valor ao numero x: ");
	scanf(" %d",&x); /*esse comando e uma entrada de dados,nesse caso ele esperar um valor "%d" que e um numero inteiro e vai armazenar na variavel acompanhnado 
com & que nesse caso e a variavel x. o usuario que vai fornecer a entrada de dados atraves da escrita*/
printf("%d",x);
}
